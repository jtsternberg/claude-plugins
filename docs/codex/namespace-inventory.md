# Codex namespace inventory

Generated by `node scripts/inventory-codex-namespaces.mjs`. It is a read-only
snapshot of public skill frontmatter and public marketplace/plugin manifests.
It deliberately does not read credentials, tokens, or general Codex config.

## Discovery sources

The generator checks six live source types: workspace/ancestor
`.agents/skills`, user `.agents/skills`, user `.codex/skills`, Codex system
skills, administrator skills, and enabled plugin caches. An explicit
`--skill-root` adds a test or exceptional standalone source. Plugin caches are
selected from enabled `codex plugin list` records, so stale cached versions do
not inflate the live inventory.

| Source | Skills | Root |
| --- | ---: | --- |
| workspace .agents/skills | 0 | `<WORKSPACE>/.agents/skills` |
| user .agents/skills | 24 | `<HOME>/.agents/skills` |
| user .codex/skills | 70 | `<HOME>/.codex/skills` |
| Codex system skills | 6 | `<HOME>/.codex/skills/.system` |
| administrator skills | 0 | `<EXTERNAL>` |
| enabled plugin cache | 117 | `<CODEX_HOME>/plugins/cache (enabled plugins only)` |

Enabled plugins: 32. Discovered skills: 217. Skill-name collisions: 16.

## Skill-name collisions

| Name | Sources | Disposition |
| --- | --- | --- |
| `agents-sdk` | cloudflare@claude-plugins-official<br>user .agents/skills<br>user .codex/skills | accept: plugin/system entries are attributed; consolidate duplicate standalone copies before relying on an unqualified invocation |
| `asana-task-manager` | asana-cli@asana-cli<br>user .codex/skills | accept: plugin/system entries are attributed; consolidate duplicate standalone copies before relying on an unqualified invocation |
| `beads-troubleshooting` | user .agents/skills<br>user .codex/skills | consolidate: keep one documented standalone root (.agents/skills) and remove or rename the other local copy |
| `brainstorming` | superpowers@claude-plugins-official<br>user .codex/skills | accept: plugin/system entries are attributed; consolidate duplicate standalone copies before relying on an unqualified invocation |
| `cloudflare` | cloudflare@claude-plugins-official<br>user .agents/skills<br>user .codex/skills | accept: plugin/system entries are attributed; consolidate duplicate standalone copies before relying on an unqualified invocation |
| `cloudflare-email-service` | cloudflare@claude-plugins-official<br>user .codex/skills | accept: plugin/system entries are attributed; consolidate duplicate standalone copies before relying on an unqualified invocation |
| `cloudflare-one` | cloudflare@claude-plugins-official<br>user .agents/skills<br>user .codex/skills | accept: plugin/system entries are attributed; consolidate duplicate standalone copies before relying on an unqualified invocation |
| `cloudflare-one-migrations` | cloudflare@claude-plugins-official<br>user .agents/skills<br>user .codex/skills | accept: plugin/system entries are attributed; consolidate duplicate standalone copies before relying on an unqualified invocation |
| `durable-objects` | cloudflare@claude-plugins-official<br>user .agents/skills<br>user .codex/skills | accept: plugin/system entries are attributed; consolidate duplicate standalone copies before relying on an unqualified invocation |
| `frontend-design` | frontend-design@claude-plugins-official<br>user .codex/skills | accept: plugin/system entries are attributed; consolidate duplicate standalone copies before relying on an unqualified invocation |
| `sandbox-sdk` | user .agents/skills<br>user .codex/skills | consolidate: keep one documented standalone root (.agents/skills) and remove or rename the other local copy |
| `skill-creator` | Codex system skills<br>skill-creator@claude-plugins-official<br>user .codex/skills | accept: plugin/system entries are attributed; consolidate duplicate standalone copies before relying on an unqualified invocation |
| `turnstile-spin` | cloudflare@claude-plugins-official<br>user .codex/skills | accept: plugin/system entries are attributed; consolidate duplicate standalone copies before relying on an unqualified invocation |
| `web-perf` | cloudflare@claude-plugins-official<br>user .agents/skills<br>user .codex/skills | accept: plugin/system entries are attributed; consolidate duplicate standalone copies before relying on an unqualified invocation |
| `workers-best-practices` | cloudflare@claude-plugins-official<br>user .agents/skills<br>user .codex/skills | accept: plugin/system entries are attributed; consolidate duplicate standalone copies before relying on an unqualified invocation |
| `wrangler` | cloudflare@claude-plugins-official<br>user .agents/skills<br>user .codex/skills | accept: plugin/system entries are attributed; consolidate duplicate standalone copies before relying on an unqualified invocation |
Plugin entries have a source label in the Codex selector; that makes a
plugin-vs-plugin collision an attribution concern, not a blind choice. The
unqualified risk is duplicate standalone copies. The disposition above therefore
consolidates local copies under the documented `.agents/skills` root without
renaming repository skills or changing an installed configuration.

## Marketplace plugin-name collisions

Configured marketplaces: 13. Public catalog entries read: 490. Plugin-name collisions: 37.

| Name | Sources | Disposition |
| --- | --- | --- |
| `airtable` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `amplitude` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `apollo` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `asana` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `base44` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `box` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `canva` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `carta-crm` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `catalyst-by-zoho` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `circleback` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `cloudflare` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `cloudinary` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `coderabbit` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `codex` | jtsternberg<br>openai-codex | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `convex` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `datadog` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `expo` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `figma` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `github` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `hostinger` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `hyperframes` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `intercom` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `linear` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `lovable` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `notion` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `posthog` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `render` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `sentry` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `slack` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `stripe` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `supabase` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `superpowers` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `twilio-developer-kit` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `vercel` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `windsor-ai` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `wix` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |
| `zoominfo` | claude-plugins-official<br>openai-curated | accept: marketplace-qualified plugin IDs are unambiguous; keep the marketplace names |

## Marketplace-name decision

Keep `jtsternberg`. It is a distinctive publisher identity, already embedded in
published `plugin@marketplace` references, and it does not collide with another
configured marketplace. The `codex` plugin-name collision is safely expressed
as `codex@jtsternberg` versus `codex@openai-codex`; it is not evidence that
the marketplace should be renamed.

## Marketplace catalog coverage

| Marketplace | Entries | Source |
| --- | ---: | --- |
| aiqrank | 1 | .claude-plugin/marketplace.json |
| asana-cli | 1 | .claude-plugin/marketplace.json |
| beads-marketplace | 1 | .claude-plugin/marketplace.json |
| buddy-marketplace | 1 | .claude-plugin/marketplace.json |
| claude-plugins-official | 287 | .claude-plugin/marketplace.json |
| compound-engineering-plugin | 1 | .agents/plugins/marketplace.json |
| jtsternberg | 3 | .agents/plugins/marketplace.json |
| openai-bundled | 7 | .agents/plugins/marketplace.json |
| openai-codex | 1 | .claude-plugin/marketplace.json |
| openai-curated | 180 | .agents/plugins/marketplace.json |
| openai-primary-runtime | 5 | .agents/plugins/marketplace.json |
| snippets-cli | 1 | .claude-plugin/marketplace.json |
| tjdsneto | 1 | .claude-plugin/marketplace.json |
